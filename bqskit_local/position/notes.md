To be part of an executable_cluster:

each PositionLabel has EXECUTE 
each EdgeLabel has EXECUTE in both directions (edgeLabel execution is like a gate) (Does not Need every edge combination) think about this in context of SABRE

each EdgeLabel has (MOVE or SWAP) 

each weight represents some idea of a cost? I see some rustworkx functions use the weights, I'm not sure how exactly though. 


first look aty an IBMQ machine machine to model / w/ it's connectivity graph, use SABRE on that. 

