Benchmark circuit resolver used for SABRE experiments.

The superconducting SABRE benchmark entry points import this package to find benchmark
QASM circuits. The canonical circuits live in the repository-level
`benchmark_circuits/` directory so data files are kept outside of the
`bqskit/` package namespace.
